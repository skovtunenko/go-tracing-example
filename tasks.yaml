tasks:
  run:
    description: Run the application
    steps:
      - run: docker compose up -d
      - run: go run cmd/main.go
  down:
    description: Tear down the application
    steps:
      - run: docker compose down
